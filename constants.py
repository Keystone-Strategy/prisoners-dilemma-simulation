payoffs = [
    [(-1, -1), (-3, 0)],
    [(0, -3), (-2, -2)]
]

rounds = 100

# If we're doing random rounds, we use a min and maximum
min_rounds = 1
max_rounds = 200

agents_dir = 'agents'
